import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const FilterZoomBox = styled(Box)`
  display: flex;
  align-items: center;
  flex-direction: column;
  padding: 8px 16px;
`;